% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_wrapper.R
\encoding{UTF-8}
\name{track_emissions_for}
\alias{track_emissions_for}
\title{Track Emissions for an Expression or Task}
\usage{
track_emissions_for(task_name, expr)
}
\arguments{
\item{task_name}{A short string describing the task (e.g., `"train_model"`).
Used as the `project_name` in the metadata.}

\item{expr}{An R expression to evaluate (e.g., a block of code to measure).}
}
\value{
The result of evaluating `expr`, invisibly.
}
\description{
Wraps a block of code (expression) to track execution time and estimate
}
\details{
Track Emissions for an Expression or Task
}
\examples{
track_emissions_for("example_sleep", {
  Sys.sleep(2)
})

if (requireNamespace("randomForest", quietly = TRUE)) {
  track_emissions_for("iris_rf", {
    model <- randomForest::randomForest(Species ~ ., data = iris)
  })
}
}
